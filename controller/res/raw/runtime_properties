#
# DEFAULT (dev) runtime_properties
#

# These items are all accessible at runtime via RuntimeLoader 
# (which is created in the Application object).
# 
# NOTE: if an ANT env aware build is run this file is REPLACED by the env specific version.
# (see runtime.properties.dev, runtime.properties.stage, and runtime.properties.prod for env specific versions)
# (when ant is NOT run, the values seen here are used as is) 
#

env=DEV

# google analytics (NULL in dev so stats aren't sent)
#google.analytics.id=
